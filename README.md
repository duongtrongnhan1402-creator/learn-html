# What You Need to Learn About HTML

## ✅ 1. Basics of HTML
- What HTML is and how it works  
- Structure of an HTML document (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`)  
- Elements, tags, and attributes  
- How browsers read HTML  

---

## ✅ 2. Essential HTML Tags

### **Text & Content**
- `<h1>`–`<h6>` (headings)
- `<p>` (paragraphs)
- `<strong>`, `<em>` (bold/italic)
- `<br>` (line break)

### **Links & Media**
- `<a>` (links)
- `<img>` (images)
- `<audio>`, `<video>` (media)

### **Lists**
- `<ul>` (unordered list)
- `<ol>` (ordered list)
- `<li>` (list item)

### **Tables**
- `<table>`
- `<tr>`
- `<td>`
- `<th>`
- `<thead>`
- `<tbody>`

### **Forms**
- `<form>`
- `<input>`
- `<textarea>`
- `<select>`
- `<option>`
- `<button>`

---

## ✅ 3. Page Structure & Layout
- `<header>`
- `<nav>`
- `<section>`
- `<article>`
- `<aside>`
- `<footer>`
- `<div>` (block-level container)
- `<span>` (inline container)

---

## ✅ 4. Important Attributes
- `id`
- `class`
- `src`
- `href`
- `alt`
- `type`
- `value`
- `placeholder`
- `style` (usually replaced by CSS)

---

## ✅ 5. Semantic HTML
- `<main>`
- `<figure>` / `<figcaption>`
- `<time>`
- Correct heading structure (`h1`, `h2`, etc.)

---

## ✅ 6. Accessibility Basics
- `alt` text for images
- ARIA roles (basic)
- Form labels and input associations

---

## ✅ 7. How HTML Works with CSS & JavaScript
- CSS = styling  
- JavaScript = interaction  
- Use `<link>` for CSS  
- Use `<script>` for JS  

---

## 📘 Bonus: Tools to Practice
- VS Code  
- Browser DevTools  
- MDN Web Docs  
- W3Schools  
- CodePen / Replit / JSFiddle  

